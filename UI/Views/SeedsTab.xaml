<UserControl x:Class="BananaMacro.UI.Views.SeedsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="{DynamicResource PanelBrush}" Padding="12">
    <StackPanel>
      <TextBlock Text="Seeds" Style="{StaticResource SectionTitle}"/>
      <TextBlock Text="Manage seeds here" Foreground="{DynamicResource TextBrush}" Margin="0,8,0,0"/>
    </StackPanel>
  </Grid>
</UserControl>