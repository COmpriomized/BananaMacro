<UserControl x:Class="BananaMacro.UI.Controls.MacroEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="{DynamicResource BgBrush}" Padding="8">
    <TextBox Text="{Binding Script, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Foreground="{DynamicResource TextBrush}" Background="{DynamicResource PanelAltBrush}"/>
  </Grid>
</UserControl>